# Core
streamlit>=1.32.0
opencv-python>=4.9.0
numpy>=1.24.0

# Detection + ByteTrack (ByteTrack bundled inside ultralytics — no extra install)
ultralytics>=8.1.0

# Face embedding
insightface>=0.7.3
onnxruntime>=1.17.0
# onnxruntime-gpu>=1.17.0  # swap for GPU

# Body embedding
torch>=2.1.0
torchvision>=0.16.0

# Utilities
pandas>=2.0.0
Pillow>=10.0.0
scipy>=1.11.0

# NOTE: deep-sort-realtime is NO LONGER NEEDED
# ByteTrack is bundled with ultralytics via bytetrack.yaml